# Puerto del servidor (opcional, por defecto 3000)
PORT=3000

# Clave secreta para JWT (cambiar en producción)
JWT_SECRET=tu-clave-secreta-muy-segura

# Mercado Pago - Checkout Pro (opcional; si no se configura, los pedidos se crean y se coordina pago manual)
# Obtener Access Token en https://www.mercadopago.com.ar/developers/panel/app
MERCADOPAGO_ACCESS_TOKEN=APP_USR-...
# URL pública para notificaciones de pago (opcional; por defecto se usa la misma URL del servidor + /api/webhook/mercadopago)
# MERCADOPAGO_NOTIFICATION_URL=https://tudominio.com/api/webhook/mercadopago
